version: '1.0'

steps:
  build-prj:
    type: build
    description: e2e use case
    image-name: codefreshio/yaml-node-4
    dockerfile: Dockerfile
    tag: latest

  push-dockerhub:
    type: push
    candidate: ${{build-prj}}
    tag: latest