version: '1.0'

steps:
  build-prj:
    type: build
    description: e2e use case
    image-name: codefreshio/yaml-node-4
    dockerfile: Dockerfile
    tag: latest

  push-prj:
    type: push
    candidate: ${{build-prj}}
    tag: latest
    credentials:
      username: ${{USERNAME_DOCKERHUB}}
      password: ${{PASSWORD_DOCKERHUB}}