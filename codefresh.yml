version: '1.0'

steps:
  build-prj:
    type: build
    description: e2e use case
    image-name: codefreshio/yaml-node-6
    dockerfile: Dockerfile
    tag: latest

  deploy-aws:
    image: tutum/curl
    candidate: ${{build-prj}}
    commands:
      - command