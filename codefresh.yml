version: '1.0'

steps:
   unit-test-prj:
   image: node:latest
    commands:
      - start.sh

   build-prj:
    type: build
    description: e2e use case
    image-name: codefreshio/yaml-node-9
    dockerfile: Dockerfile
    tag: latest