version: '1.0'

steps:
  clone-step:
    type: git-clone
    repo: https://github.com/codefresh-io/yaml-node-hello-world.git
    
  composition-step:
    type: composition
    composition: test-compo
    working-directory: ${{clone-step}}
    composition-candidates:
      small:
        image: 'davidfeldi/small:test1'
        command: echo \"Waiting for DB\"
